name: First workflow
on: workflow_dispatch
jobs:
  first-job:
    runs-on: ubuntu-latest
    steps:
      - name: Hello Github Action
        run: echo "Hello this is first workflow"
      - name: Bye github actions
        run: echo "Bye github actions"
